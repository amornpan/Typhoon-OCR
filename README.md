# Typhoon OCR

เครื่องมือ OCR สำหรับไฟล์ PDF โดยใช้ Typhoon OCR API ที่เหมาะสำหรับเอกสารภาษาไทย

## การสมัครและรับ API Key

### 1. สมัครบัญชี Typhoon OCR
1. ไปที่เว็บไซต์ [Typhoon OCR](https://api.opentyphoon.ai/)
2. คลิก "Sign Up" หรือ "สมัครสมาชิก"
3. กรอกข้อมูล:
   - อีเมล
   - รหัสผ่าน
   - ข้อมูลส่วนตัวที่จำเป็น
4. ยืนยันอีเมลตามที่ระบบส่งมาให้

### 2. รับ API Key
1. เข้าสู่ระบบด้วยบัญชีที่สมัครแล้ว
2. ไปที่ Dashboard หรือ API Settings
3. คลิก "Create New API Key" หรือ "สร้าง API Key ใหม่"
4. ตั้งชื่อ API Key (เช่น "OCR-Project")
5. คัดลอก API Key ที่ได้รับ (เก็บไว้อย่างปลอดภัย)

## การติดตั้ง

### 1. สร้าง Conda Environment (แนะนำ)
```bash
conda create -n typhoon-ocr python=3.9
conda activate typhoon-ocr
```

### 2. ติดตั้ง Dependencies
```bash
pip install -r requirements.txt
```

### 3. ตั้งค่า API Key
สร้างไฟล์ `.env` และใส่ API Key:
```bash
# วิธีที่ 1: ใช้ echo command
echo "TYPHOON_OCR_API_KEY=your_api_key_here" > .env

# วิธีที่ 2: สร้างไฟล์ .env ด้วยมือ
cp .env.example .env
# แล้วแก้ไขไฟล์ .env ใส่ API Key จริง
```

## โครงสร้างโปรเจกต์
```
Typhoon-OCR/
├── ocr.py              # ไฟล์หลักสำหรับ OCR
├── pdf_splitter.py     # เครื่องมือแยกหน้า PDF
├── requirements.txt    # รายการ dependencies
├── .env               # ไฟล์เก็บ API Key (ไม่ควร commit)
├── .env.example       # ตัวอย่างไฟล์ .env
├── README.md          # เอกสารนี้
├── pdf_corpus/        # โฟลเดอร์เก็บไฟล์ PDF
│   └── สิ่งที่ส่งมาด้วย-2-เอกสารแนบท้ายประกาศ-3.1-Final-DQAF-v1.0.pdf
├── output/            # โฟลเดอร์เก็บผลลัพธ์ OCR
└── temp_pages/        # โฟลเดอร์เก็บรูปภาพหน้า PDF (ถ้าใช้ pdf_splitter)
```

## วิธีการใช้งาน

### การ OCR ไฟล์ PDF

#### 1. ทดสอบหน้าแรก (แนะนำสำหรับครั้งแรก)
```bash
python ocr.py 1
```

#### 2. ประมวลผล 3 หน้าแรก
```bash
python ocr.py 3
```

#### 3. ประมวลผลทุกหน้า (25 หน้า)
```bash
python ocr.py all
```

### การแยกหน้า PDF เป็นรูปภาพ (เพิ่มเติม)
```bash
# แยกหน้าที่ 1-3
python pdf_splitter.py pdf_corpus/ชื่อไฟล์.pdf 1,2,3

# แยกทุกหน้า
python pdf_splitter.py pdf_corpus/ชื่อไฟล์.pdf
```

## ผลลัพธ์

### ตำแหน่งไฟล์ผลลัพธ์
ผลลัพธ์จะถูกบันทึกในโฟลเดอร์ `output/` ในรูปแบบ:
- `output/ocr_YYYYMMDD_HHMMSS.md`
- เช่น: `output/ocr_20241123_143052.md`

### รูปแบบผลลัพธ์
ไฟล์ผลลัพธ์จะเป็น Markdown format ที่ประกอบด้วย:
- ข้อมูลไฟล์ต้นฉบับ
- วันที่ประมวลผล  
- เนื้อหา OCR แยกตามหน้า
- ข้อผิดพลาด (ถ้ามี)

## ไฟล์ PDF ตัวอย่าง

ในโปรเจกต์นี้มีไฟล์ PDF ตัวอย่าง:
- **ชื่อไฟล์**: สิ่งที่ส่งมาด้วย-2-เอกสารแนบท้ายประกาศ-3.1-Final-DQAF-v1.0.pdf
- **จำนวนหน้า**: 25 หน้า
- **ประเภท**: เอกสารราชการภาษาไทย
- **เหมาะสำหรับ**: ทดสอบ Typhoon OCR เพราะเป็นเอกสารไทยที่มีรูปแบบซับซ้อน

## การแก้ปัญหาเบื้องต้น

### ❌ ปัญหา: "No API key found"
**วิธีแก้**:
1. ตรวจสอบว่าไฟล์ `.env` อยู่ในโฟลเดอร์โปรเจกต์
2. ตรวจสอบว่า API Key ในไฟล์ `.env` ถูกต้อง
3. ลองรีสตาร์ท terminal หรือ conda environment

### ❌ ปัญหา: "PDF not found"
**วิธีแก้**:
1. ตรวจสอบว่าไฟล์ PDF อยู่ในโฟลเดอร์ `pdf_corpus/`
2. ตรวจสอบชื่อไฟล์ในโค้ด `ocr.py` ว่าตรงกับไฟล์จริง

### ❌ ปัญหา: "API rate limit exceeded"
**วิธีแก้**:
1. รอสักครู่แล้วลองใหม่
2. ลดจำนวนหน้าที่ประมวลผลต่อครั้ง
3. ตรวจสอบโควต้า API ในบัญชีของคุณ

## Dependencies

ไลบรารี่ที่ใช้ในโปรเจกต์:
- **typhoon-ocr**: API สำหรับ OCR ภาษาไทย
- **python-dotenv**: จัดการ environment variables
- **PyMuPDF**: จัดการและอ่านไฟล์ PDF
- **tqdm**: แสดง progress bar ขณะประมวลผล
- **colorama**: จัดการสีในหน้า terminal

## ข้อดีของ Typhoon OCR

✅ **เหมาะกับภาษาไทย**: ออกแบบมาเฉพาะสำหรับข้อความภาษาไทย  
✅ **ความแม่นยำสูง**: ผลลัพธ์ดีกว่า OCR ทั่วไป  
✅ **รองรับเอกสารซับซ้อน**: ตารางข้อมูล, เอกสารราชการ  
✅ **ใช้งานง่าย**: API เรียบง่าย ติดตั้งได้เร็ว  
✅ **ประหยัดเวลา**: ประมวลผลเร็วกว่าการพิมพ์ใหม่  

## เวอร์ชัน

- **Python**: 3.8+ (แนะนำ 3.9)
- **Typhoon OCR**: เวอร์ชันล่าสุด
- **OS**: Windows, macOS, Linux

---

🚀 **พร้อมใช้งาน!** เริ่มต้นด้วย `python ocr.py 1` เพื่อทดสอบหน้าแรก
